// This middleware is no longer needed - mock handlers are loaded via server/plugins/init-mocks.ts
// Kept for reference only

export default defineEventHandler(() => {
  // Handlers are initialized in the Nitro plugin
})

